```js
// Optional JS for dynamic interactions
console.log('Admin panel loaded');

document.addEventListener('DOMContentLoaded', () => {
  // Future logic for dynamic loading or interactions
});
```

---

✅ Link them in `index.html` and `login.html`:

Add inside `<head>`:
```html
<link rel="stylesheet" href="/css/style.css" />
```

Add before `</body>`:
```html
<script src="/js/main.js"></script>
```
